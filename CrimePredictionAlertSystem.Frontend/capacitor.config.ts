import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mthokozisi.crimealert',
  appName: 'CrimeAlert',
  webDir: 'dist/crime-prediction-alert-system.frontend/browser'
};

export default config;
